const reducer = (state,action)=>{
    const{type,payload} = action;
    switch (type) {
        case SET_ALERT:
            
            break;
    
        default:
            return state
    }
}

export default reducer;